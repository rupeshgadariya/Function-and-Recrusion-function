// Write a program in C to count the digits of a given number using recursion. 
#include<stdio.h>
#include<conio.h>
int count(int num)
{
    if(num==0)  return 0;
    return 1+count(num/10);
}


    void main()
{
    printf("%d",count(481589));        
        
    getch();
}
