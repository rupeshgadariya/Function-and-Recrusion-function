// Write a recursive function to calculate sum of first N natural numbers
#include<stdio.h>
#include<conio.h>
int sum(int n)
{
    if(n==1) return 1;
    return n+sum(n-1);
    
}

    void main()
{
        
 printf("%d",sum(6));       
    getch();
}
