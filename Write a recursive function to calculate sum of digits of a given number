// Write a recursive function to calculate sum of digits of a given number
#include<stdio.h>
#include<conio.h>
int sum(int n)
{
    if(n==0) return 0;

    return n%10 + sum(n/10);
}


    void main()
{
      printf("%d",sum(49614899));  
        
    getch();
}
