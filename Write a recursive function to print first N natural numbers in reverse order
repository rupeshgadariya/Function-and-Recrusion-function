// Write a recursive function to print first N natural numbers in reverse order 
#include<stdio.h>
#include<conio.h>

void rev(int n)
{
    if (n==0)
     return;
     printf("%d  ",n);
     rev(n-1);
}


    void main()
{
        rev(15);
        
        
    getch();
}
