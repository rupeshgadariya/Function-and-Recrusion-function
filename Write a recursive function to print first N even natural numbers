// Write a recursive function to print first N even natural numbers 
#include<stdio.h>
#include<conio.h>
void even(int n)
{
    if(n==0) return;
    even(n-1);
    printf("%d  ",2*n);
}

    void main()
{
        even(10);
        
    getch();
}
